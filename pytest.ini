[pytest]
testpaths = tests
addopts = -ra --strict-markers --tb=short
markers =
    slow: tests that touch real git or subprocesses (deselect with -m "not slow")
    integration: end-to-end tests that exercise full CLI paths
